"use strict";

const kanaDisplay: HTMLDivElement = <HTMLDivElement>(
  document.getElementById("kanaDisplay")
);
const kanaForm: HTMLFormElement = <HTMLFormElement>(
  document.getElementById("kanaForm")
);

window.onload = (): void => {
  initialize();
};

const initialize = (): void => {};
